# Core dependencies
fastapi==0.103.1
uvicorn==0.23.2
websockets==11.0.3
numpy==1.25.2
scipy==1.11.2
python-multipart==0.0.6
python-dotenv==1.0.0
asyncio==3.4.3
colorama==0.4.6
psutil==5.9.5

# Audio processing
faster-whisper==0.9.0
torch==2.0.1

# Translation options - Comment one of these sections based on your preference
# Option 1: Standard translation with Hugging Face (higher quality, larger download)
# transformers==4.33.1

# Option 2: Fallback translation with deep-translator (smaller, requires internet)
deep-translator==1.11.4

# To switch between options, run:
# python backend/use_fallback.py